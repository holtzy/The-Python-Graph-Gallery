import React from "react";
import './parralax.css'

export default function Parralax({ children }) {
    return (
        <div className="parralaxSection">
            {children}
        </div>
    );
}
