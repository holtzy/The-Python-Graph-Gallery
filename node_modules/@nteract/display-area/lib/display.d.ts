import * as React from "react";
import { Transforms } from "@nteract/transforms";
import { Map as ImmutableMap } from "immutable";
declare type Props = {
    displayOrder: string[];
    outputs: any[];
    transforms: Transforms;
    theme: string;
    expanded: boolean;
    isHidden: boolean;
    models: Object;
};
export declare const DEFAULT_SCROLL_HEIGHT = 600;
export default class Display extends React.PureComponent<Props> {
    static defaultProps: {
        transforms: any;
        displayOrder: string[];
        isHidden: boolean;
        expanded: boolean;
        theme: string;
        models: ImmutableMap<{}, {}>;
    };
    render(): JSX.Element | null;
}
export {};
